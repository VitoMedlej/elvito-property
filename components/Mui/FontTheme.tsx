import { createTheme } from "@mui/material"

const theme = createTheme({
    typography: {
        allVariants: {
            fontFamily: 'Raleway, sans-serif',
            fontSize: '16px'
        }
    }
})
export {theme}